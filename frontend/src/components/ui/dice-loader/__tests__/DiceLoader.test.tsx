/**
 * DiceLoader tests
 * NOTE: Skipped in CI - requires WebGL support
 */

import { describe, it, expect } from 'vitest';

describe.skip('DiceLoader', () => {
  it.skip('requires WebGL context', () => {
    expect(true).toBe(true);
  });
});
